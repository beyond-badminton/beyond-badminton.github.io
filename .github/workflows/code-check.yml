name: Code Quality Check

on:
  pull_request:
    branches: [ master ]

jobs:
  validate-site:
    name: HTML & Site Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify index.html exists
        run: |
          if [ ! -f index.html ]; then
            echo "CRITICAL ERROR: index.html is missing from the root directory!"
            exit 1
          fi
          echo "index.html found."

      - name: Check HTML syntax
        run: npx htmlhint **/*.html

      - name: Check HTML & JS & CSS Quality
        env:
          FORCE_COLOR: "3"
        run: npx @biomejs/biome check . --colors=force